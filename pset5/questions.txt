0.  Термин, обозначающий явное воспаление легких, обусловленное вдыханием кварцевой пыли
1.  Функция возвращает ограничение на ресурсы для своего первого аргумента
2.  16 полей в struct rusage 2 из них отдельные структуры
3.  Потому что нам нужен адрес этих переменных, ведь мы записываем значение в поле по конкретному адресу, по аналогии с сканф, и калькулейт принимает как аргументы адреса структурной переменной, все суть в том, что нам надо изменить не копии переменных, которые создат функция как объекты стека, а значение конкретных объектов из функции мейн
4.  Мейн обращается к фгетс, в качество потока выбирает не поток ввода, а фаил, соответственно фгетс считывает байты файла перемещая указатель, пока не встретит EOF
5.  Слово может содержать пробелы и иные символы, которые стопят работу сканф, к тому же, мы отдельно проверяем каждый символ, сканф вынудил бы нас встраивать ту же самую проверку в цикл с параметром и переписывать массив
6.  Чтобы гарантировано избежать изменение строки и не просчитать значения загрузки или чека как что-то другое, или вовсе спровоцировать ошибку сегментации. Так же мы запрещаем менять адрес переменной указателя, чтобы не спровоцировать ошибок при обработке слов и выписать в итоге правильный указатель, к тому же мы должны знать, что слово в статистике выведется с первого символа и мы не будем инкрементировать указатель;
7.  TODO
8.  TODO
9.  TODO
10. TODO
